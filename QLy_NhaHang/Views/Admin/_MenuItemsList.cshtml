@model List<QLy_NhaHang.Models.MenuItem>
@{
    var categoryName = ViewBag.CategoryName as string ?? "Tất cả";
    var type = ViewBag.Type as string ?? "";
}

<div class="menu-items-list-container">
    <div class="mb-3">
        @await Html.PartialAsync("_AdminBreadcrumb")
    </div>
    
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="mb-0">
            <i class="bi bi-list-ul me-2"></i>
            @if (categoryName != "Tất cả")
            {
                <span>Danh Sách @categoryName</span>
            }
            else
            {
                <span>Danh Sách Món Ăn</span>
            }
        </h2>
        <div class="btn-group" role="group">
            <button type="button" class="btn btn-success" onclick="openCreateModal('mon-an')">
                <i class="bi bi-egg-fried me-2"></i>Thêm Món Ăn
            </button>
            <button type="button" class="btn btn-info" onclick="openCreateModal('thuc-uong')">
                <i class="bi bi-cup-straw me-2"></i>Thêm Thức Uống
            </button>
            <button type="button" class="btn btn-danger" onclick="openCreateModal('ruou')">
                <i class="bi bi-wine me-2"></i>Thêm Rượu
            </button>
        </div>
    </div>

    @if (Model != null && Model.Any())
    {
        <div class="card shadow-sm">
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-hover align-middle">
                        <thead class="table-light">
                            <tr>
                                <th style="width: 100px;">Ảnh</th>
                                <th>Tên Món Ăn</th>
                                <th>Danh Mục</th>
                                <th>Giá</th>
                                <th>Đơn Vị</th>
                                <th>Trạng Thái</th>
                                <th style="width: 150px;" class="text-center">Thao Tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @if (!string.IsNullOrEmpty(item.ImageUrl))
                                        {
                                            <img src="@item.ImageUrl" 
                                                 alt="@item.Name" 
                                                 class="img-thumbnail" 
                                                 style="width: 80px; height: 80px; object-fit: cover;" />
                                        }
                                        else
                                        {
                                            <div class="bg-light d-flex align-items-center justify-content-center" 
                                                 style="width: 80px; height: 80px; border-radius: 0.25rem;">
                                                <i class="bi bi-image text-muted" style="font-size: 2rem;"></i>
                                            </div>
                                        }
                                    </td>
                                    <td>
                                        <strong>@item.Name</strong>
                                        @if (!string.IsNullOrEmpty(item.Description))
                                        {
                                            <br />
                                            <small class="text-muted">@(item.Description.Length > 50 ? item.Description.Substring(0, 50) + "..." : item.Description)</small>
                                        }
                                    </td>
                                    <td>
                                        @if (item.Category != null)
                                        {
                                            <span class="badge bg-info">@item.Category.Name</span>
                                        }
                                        else
                                        {
                                            <span class="text-muted">Chưa phân loại</span>
                                        }
                                    </td>
                                    <td>
                                        <strong class="text-danger">@item.Price.ToString("N0") đ</strong>
                                    </td>
                                    <td>@item.Unit</td>
                                    <td>
                                        @if (item.IsActive)
                                        {
                                            <span class="badge bg-success">Đang hoạt động</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">Tạm ngưng</span>
                                        }
                                    </td>
                                    <td class="text-center">
                                        <div class="btn-group" role="group">
                                            <a asp-action="EditMenuItem" asp-route-id="@item.Id" 
                                               class="btn btn-sm btn-outline-primary" 
                                               title="Chỉnh sửa">
                                                <i class="bi bi-pencil"></i>
                                            </a>
                                            <button type="button" 
                                                    class="btn btn-sm btn-outline-danger" 
                                                    onclick="confirmDelete(@item.Id, '@item.Name')"
                                                    title="Xóa">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="card shadow-sm">
            <div class="card-body text-center py-5">
                <i class="bi bi-inbox display-1 text-muted mb-3"></i>
                <h4 class="text-muted mb-3">Chưa có @categoryName.ToLower() nào</h4>
                <p class="text-muted mb-4">Hãy thêm @categoryName.ToLower() đầu tiên để bắt đầu.</p>
                <button type="button" class="btn btn-primary" onclick="openCreateModal('@type')">
                    <i class="bi bi-plus-circle me-2"></i>Thêm @categoryName Đầu Tiên
                </button>
            </div>
        </div>
    }
</div>

